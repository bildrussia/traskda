# Telegram-бот с интеграцией ИИ

Telegram-бот на Python с использованием aiogram 3.x и интеграцией ИИ через OpenRouter API.

## Возможности

- 💬 Отвечает на текстовые сообщения через ИИ
- 🧠 Память диалога (последние 10 сообщений на пользователя)
- ⌨️ Индикатор "печатает..." во время генерации ответа
- 🔒 Безопасное хранение ключей в .env файле

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Настройте `.env` файл:
   - Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram
   - Вставьте токен в `TELEGRAM_TOKEN` в файле `.env`
   - API ключ OpenRouter уже настроен (можно изменить при необходимости)
   - При необходимости измените модель ИИ в `OPENROUTER_MODEL`

## Запуск

```bash
python main.py
```

## Команды бота

- `/start` - Начать новый диалог (очистить историю сообщений)
- `/help` - Показать справку

## Используемые технологии

- **aiogram 3.x** - Асинхронный фреймворк для Telegram Bot API
- **OpenAI Python SDK** - Клиент для работы с OpenRouter API
- **python-dotenv** - Загрузка переменных окружения

## Модели ИИ

По умолчанию используется `google/gemini-pro-1.5`. Можно изменить в `.env` файле на:
- `anthropic/claude-3.5-sonnet`
- Или любую другую модель, доступную на OpenRouter

## Структура проекта

```
.
├── main.py              # Основной код бота
├── .env                 # Переменные окружения (не коммитится)
├── requirements.txt     # Зависимости проекта
├── .gitignore          # Игнорируемые файлы
└── README.md           # Документация
```
